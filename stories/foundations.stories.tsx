import React from 'react';
import { ColorPalette } from '../src';

export default {
  title: 'Foundations',
};

export const colorPalette = () => <ColorPalette />;

colorPalette.story = {
  name: 'Color Palette',
};
