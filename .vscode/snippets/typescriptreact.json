{
  "Value Array Prop": {
    "prefix": "ValueArrayProp",
    "body": [
      "//#region ${1:PropName}",
      "export const ${2:PROP_SINGULAR_CONSTANT} = ['${4:value1}'] as const;",
      "export type T${3:PropName} = typeof ${2:PROP_SINGULAR_CONSTANT}[number];",
      "//#endregion ${1:PropName}"
    ],
    "description": "Template to define a type based on an array of string values"
  }
}
