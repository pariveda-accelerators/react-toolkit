{
  "buildpacks": [
    {
      "url": "heroku/nodejs"
    }
  ],
  "scripts": {
    "build": "tsdx build && yarn run-s build-styles build-storybook",
    "postdeploy": "bundle exec build-storybook -c .storybook -o dist/storybook-web"
  }
}
